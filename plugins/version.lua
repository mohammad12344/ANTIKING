do

function run(msg, matches)
  return 'anti king v1.0 it is one of the best anti spams for telegram.you can use it for super groups and groups.it is for BLACK BOYS admin:@blackboys_admin_1 @erfan_fucker_you' 
end

return {
  patterns = {
    "^#version$"
  }, 
  run = run 
}

end
